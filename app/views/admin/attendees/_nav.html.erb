<% content_for :sub_nav do %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to "Attendees", admin_attendees_path, class: 'navbar-brand' %>
        <% if @attendee.present? && @attendee.persisted? %>
          <%= link_to "##{@attendee.id} -  #{@attendee.name}", admin_attendee_path(@attendee),  class: 'navbar-brand' %>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSubNav">
          <ul class="navbar-nav me-auto">
            <% if @attendee.present? && @attendee.persisted? %>
            <li class="nav-item">
              <%= link_to "Edit", edit_admin_attendee_path(@attendee), class: "nav-link" %>
            </li>
            <% end %>
            <li class="nav-item">
              <%= link_to "List", admin_attendees_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "New", new_admin_attendee_path, class: "nav-link" %>
            </li>
          </ul>
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn" %>
            </li>
        </ul>
        </div>
      </div>
    </nav>
<% end %>