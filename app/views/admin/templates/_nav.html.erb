<% content_for :sub_nav do %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to "Templates", admin_templates_path, class: 'navbar-brand' %>
        <% if @template.present? && @template.persisted? %>
          <%= link_to "##{@template.id} -  #{@template.name}", admin_template_path(@template),  class: 'navbar-brand' %>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSubNav">
          <ul class="navbar-nav me-auto">
            <% if @template.present? && @template.persisted? %>
            <li class="nav-item">
              <%= link_to "Edit", edit_admin_template_path(@template), class: "nav-link" %>
            </li>
            <% end %>
            <li class="nav-item">
              <%= link_to "List", admin_templates_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "New", new_admin_template_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
<% end %>